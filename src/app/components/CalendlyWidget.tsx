'use client';

import React from 'react';
import { InlineWidget } from 'react-calendly';

export default function CalendlyWidget() {
  // Чтобы вернуть виджет: удали return null; и раскомментируй код ниже
  return null;
  /*return (
    <section className="w-full py-10 bg-gray-50 flex flex-col items-center justify-center" id="booking">
      <h2 className="text-2xl md:text-3xl font-bold mb-6 text-center text-gray-800">
        Book Service
      </h2>
      <div className="w-full max-w-4xl min-h-[700px]">
        <InlineWidget 
          url="https://calendly.com/smaexpert/in-home_diagnostic_visit" 
          styles={{ height: '700px', width: '100%' }}
        />
      </div>
    </section>
  );*/
}